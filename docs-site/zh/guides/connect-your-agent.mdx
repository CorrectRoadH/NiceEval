---
title: "接入你的 Agent"
sidebarTitle: "接入 Agent"
description: "接入的全景：写一个 adapter、配一个 experiment、跑通第一条 eval；配置和参数怎么在 experiment 里声明、在 Adapter 里消费。事件流、多轮、HITL、tracing 是增量，各有专章。"
---

把一个被测对象接进 NiceEval，就是给它写一个 **adapter**：`defineAgent` 包住一个 `send` 函数——接一次输入，驱动你的 agent，返回这一轮结果。接入的全部工作就这一个文件。

本文只回答两个问题：**这个文件和它的两样配套（experiment、eval）怎么跑通**，以及**参数怎么从 experiment 流到 Adapter、再流到你的应用**。事件流、多轮、HITL、tracing 都是之后的可选增量，各有专章，文末给了一张地图。

## 先选路：你的被测对象是什么？

<CardGroup cols={3}>
  <Card title="AI SDK 应用" icon="bolt" href="/zh/reference/builtin-agents">
    用 Vercel AI SDK（`useChat` 后端）写的应用：内置 `uiMessageStreamAgent` 无侵入接它的 HTTP 接口，零映射、含 HITL，有完整的 before/after 示例。不用读本文。
  </Card>
  <Card title="Coding agent CLI" icon="terminal" href="/zh/guides/sandbox-agent">
    评 claude-code / codex / bub 这类改文件的 coding agent：用内置 sandbox agent。
  </Card>
  <Card title="其它 AI Agent" icon="plug">
    自研 agent loop、LangGraph / OpenAI Agents SDK 应用、已部署 agent：优先[OTel 接入](/zh/guides/connect-otel)；接不了 OTel 再手写 send。
  </Card>
</CardGroup>

## 跑通最小接入

假设项目已经 `npx niceeval init` 过（有 `niceeval.config.ts` 和 `evals/` 目录）。三个文件各管一件事：**adapter 连被测系统，experiment 定评谁怎么跑，eval 写交互和断言。**

**1. 写 adapter。** 最小接入只填 `status` 和 `events`：你的 agent 说了什么，放一条 `message` 事件。

```ts
// agents/my-bot.ts
import { defineAgent } from "niceeval/adapter";

export default defineAgent({
  name: "my-bot",
  async send(input, ctx) {
    const r = await fetch(`${process.env.MY_BOT_URL}/chat`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ message: input.text }),
      signal: ctx.signal,
    });
    const body = await r.json();
    return {
      status: r.ok ? "completed" : "failed",
      events: [{ type: "message", role: "assistant", text: body.reply }],
    };
  },
});
```

agent runtime 就在当前代码库里的话，`fetch` 换成直接调用即可。`send` 的完整契约（`TurnInput` / `AgentContext` / `Turn` 各字段）见 [Adapter](/zh/concepts/adapter)。

**2. 注册进一个 experiment：**

```ts
// experiments/my-bot.ts
import { defineExperiment } from "niceeval";
import myBot from "../agents/my-bot.ts";

export default defineExperiment({
  description: "my-bot 基线",
  agent: myBot,
  runs: 1,
});
```

**3. 写第一条 eval，然后跑：**

```ts
// evals/refund-policy.eval.ts
import { defineEval } from "niceeval";
import { includes } from "niceeval/expect";

export default defineEval({
  description: "退款政策问答",
  async test(t) {
    await t.send("你们的退款政策是什么?");
    t.succeeded();
    t.check(t.reply, includes("30 天"));
    t.judge.autoevals.closedQA("回答是否说明了退款期限?").atLeast(0.7);
  },
});
```

```bash
npx niceeval exp my-bot        # 跑这个 experiment 下的全部 eval
npx niceeval exp my-bot refund # 只跑 ID 以 refund 开头的
npx niceeval view              # 本地查看器里看结果
```

到这里接入已经完成：文本断言和 judge 评分都能用了。想要更多断言（工具、多轮、审批流），见文末的增量地图。

## 参数怎么进：experiment 声明，Adapter 消费

配置归属只有两条通道，分清它们，接入就不会乱：

1. **静态配置走 Adapter 工厂。**URL、鉴权、协议细节这类"这个环境长什么样"的配置，作为工厂参数写在 experiment 文件里——`defineExperiment` 的 `agent` 字段放的是**已经配置好的实例**。
2. **每轮动态值走 `ctx`。**experiment 声明的 `model`、`flags`，运行器每轮经 `ctx` 原样递给 `send`；Adapter 不解释它们的含义，只随请求转发给应用。

两条通道拼在一起就是完整的传参链路：

```ts
// agents/web-agent.ts —— 工厂：静态配置进闭包
import { defineAgent } from "niceeval/adapter";
import type { Agent } from "niceeval/adapter";

export function webAgent(options: { baseUrl: string; apiKey?: string }): Agent {
  return defineAgent({
    name: "web-agent",
    async send(input, ctx) {
      const res = await fetch(`${options.baseUrl}/api/turn`, {   // ← 工厂配置：连哪
        method: "POST",
        headers: {
          "content-type": "application/json",
          ...(options.apiKey ? { authorization: `Bearer ${options.apiKey}` } : {}),
        },
        body: JSON.stringify({
          message: input.text,
          model: ctx.model,     // ← experiment.model：应用接口收模型选择就转发
          flags: ctx.flags,     // ← experiment.flags：原样交给应用，应用按 flag 切换
        }),
        signal: ctx.signal,     // ← 运行器的超时与取消：挂到每个请求上
      });
      const body = await res.json();
      return { status: res.ok ? "completed" : "failed", events: body.events };
    },
  });
}
```

```ts
// experiments/staging.ts —— 参数的声明处
import { defineExperiment } from "niceeval";
import { webAgent } from "../agents/web-agent.ts";

export default defineExperiment({
  agent: webAgent({                                  // ← 静态配置：实例化时传
    baseUrl: "https://staging.example.com",
    apiKey: process.env.STAGING_AGENT_API_KEY,
  }),
  model: "gpt-5.4",                                  // ← 动态值：经 ctx.model 到 send
  flags: { promptVariant: "concise" },               // ← 动态值：经 ctx.flags 到 send
});
```

`ctx` 上每轮可能用到的字段和消费方式：

| `ctx` 字段 | 谁给的 | Adapter 怎么消费 |
|---|---|---|
| `signal` | 运行器（超时与取消） | 挂到发出的每个请求上 |
| `model` | experiment 的 `model` | 应用接口收模型选择就随请求转发；不收就忽略 |
| `flags` | experiment 的 `flags` | 原样转发（请求体、header、环境变量都行），应用按 flag 切换变体 |
| `telemetry` | 配置了 OTel 接入时出现 | `endpoint` / `headers` / `env` 交给应用或注入子进程，见 [OTel 接入](/zh/guides/connect-otel) |
| `session` | 运行器（每条会话线一份状态槽） | 一般不直接碰——官方会话件替你存取，见[写 send](/zh/guides/write-send) |
| `log(msg)` | 运行器 | 写进本轮日志，`niceeval view` 里可见 |

同一个 agent 对着本地和生产分别跑，就是两个 experiment 文件、两份工厂参数（或走环境变量）：

```bash
MY_BOT_URL=http://localhost:3000/chat npx niceeval exp local
MY_BOT_URL=https://api.example.com/chat npx niceeval exp prod
```

不要把 URL 放进 CLI 位置参数——experiment 名之后的位置参数只用于过滤 eval ID。experiment 的完整字段（`runs`、`budget`、并发、`sandbox`）见[写实验](/zh/guides/write-experiment)。

## 之后的增量：一张地图

最小接入之后，每给 adapter 加一个增量就解锁一组断言，已写的 eval 不用改：

| 想解锁什么 | 给 adapter 加什么 | 去哪看 |
|---|---|---|
| 工具断言（`calledTool` / `toolOrder` / 负断言） | 把应用返回映射成标准事件流 | [写 send](/zh/guides/write-send)、[事件流参考](/zh/reference/events) |
| 多轮对话、`t.newSession()` 隔离 | 一行会话件：`serverSession()` / `clientHistory()` | [写 send](/zh/guides/write-send) |
| 审批流（HITL，人工介入） | 停轮返回 `waiting` + `input.requested`，回答轮续跑 | [HITL](/zh/concepts/hitl) |
| 免映射事件、负断言完整性、trace 瀑布图 | 应用把 OTel span 发给 NiceEval | [OTel 接入](/zh/guides/connect-otel) |
| feature A/B 对比 | 应用把变体暴露成 `flags` 可切换的配置 | [Tier](/zh/concepts/tier)、[写实验](/zh/guides/write-experiment) |

这些增量落在哪一档、各档买到什么，见 [Tier](/zh/concepts/tier)。

## 参考实现

完整的可跑参考：五个[无侵入接入示例](/zh/example/tier1-ai-sdk-v7)，覆盖事件流断言、多轮隔离、HITL 批准 / 拒绝、OTel 派生、trace。手写 `send` 时真正要写的只有 transport 和映射表——会话续接、HITL 暂停恢复、逐帧驱动都是官方件，见[内置 Agent 能力](/zh/reference/builtin-agents)。

## 相关阅读

- [写 send](/zh/guides/write-send) —— 手写 Adapter 的完整推导：三个问题、官方件、两个完整示例。
- [Adapter](/zh/concepts/adapter) —— `send` 的契约：`TurnInput` / `AgentContext` / `Turn` 逐字段。
- [OTel 接入](/zh/guides/connect-otel) —— 已埋点应用的免映射路线（优先推荐）。
- [Tier](/zh/concepts/tier) —— 三档接入各投入什么、买到什么。
- [写实验](/zh/guides/write-experiment) —— `defineExperiment` 的完整字段。
