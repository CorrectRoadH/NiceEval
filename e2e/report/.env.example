# Real OpenAI-Chat-Completions-compatible gateway used by agents/openai-compat.ts.
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Judge credentials — not used by this repo's evals today (results asserts
# mechanism, not model quality) but declared for consistency with cli.
NICEEVAL_JUDGE_KEY=
NICEEVAL_JUDGE_BASE=
