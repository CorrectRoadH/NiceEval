# <功能名> —— 占位模板

这是 `docs/feature/<name>/` 的起始模板,复制这几个文件开始写一个新功能:`README.md` + `architecture.md` 必备,`cli.md`(有 CLI 面)和 `library.md`(纯 TS API)按功能形态二选一或都写。写完删掉本说明段。正文只写"要什么、是什么",不写实现状态。

功能是什么、解决什么问题、核心契约(API/CLI 形状)。
