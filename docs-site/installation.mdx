---
title: "Install NiceEval"
sidebarTitle: "Installation"
description: "Install NiceEval and initialize the project. The maintained setup path is the quickstart."
---

The maintained installation path lives in [Quickstart](/quickstart).

```bash
pnpm add -D niceeval
npx niceeval init
```

After initialization, add an adapter, an experiment, and an eval. See [Quickstart](/quickstart) for the complete path.
