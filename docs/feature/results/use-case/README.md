# Results —— 用例手册

本目录把 Results 的读取 API 放回实际任务中。契约单源仍在 [Library](../library.md) 与
[Architecture](../architecture.md)；用例只解释怎样选择 API，不复制字段全集或选择算法。

## 选择结果范围

- [读最近一批，还是每题的当前结果](latest-or-current.md) —— 用三个结果快照理解
  `latest()`、`current()`、覆盖缺口与 `fresh`。
