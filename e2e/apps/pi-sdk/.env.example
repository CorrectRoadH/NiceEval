# e2e 用的最小变量集(CI 由 workflow 从 secrets 写入同样内容,见 .github/workflows/e2e.yml)。
# @earendil-works/pi-ai 的 deepseekProvider() 自动读 DEEPSEEK_API_KEY。
DEEPSEEK_API_KEY=sk-replace-me
AGENT_MODEL=deepseek-v4-flash
